# AigcForge 技术债与后期任务清单

> 统一入口文档：所有已声明但未闭环的技术债、延后任务与待启动规划的归档地。
> CLAUDE.md「已知技术负债」为本文的精简引用；`docs/review/` / `docs/audit/` 为缺陷证据源。
> 更新规则：每闭环一项移入「已闭环」表并记录日期与提交；新增债必须写来源（PR/审计/计划）与触发条件。

---

## 0. 快速索引

| 分类                            | 内容                                                                                                                   | 状态                                                                                                                                                          |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| §1 权限档位遗留（PR #32）       | M1/M2/M3/M5/D5 五项                                                                                                    | 待专项处理                                                                                                                                                    |
| §2 页面归一化延后（PR #34）     | 960px 主列、Assistant scope、Chat Location 抽取、全仓 import 债                                                        | 计划外延后                                                                                                                                                    |
| §3 Custom Mode 平台（PR #33）   | ADR-17 评审 + Roadmap M0-M5；§3.1 Custom M2 遗留 12 项；§3.2 Custom M3 Phase B/D/G 遗留、F0 剩余裁定项与 ADR-21 排除项 | M0/M1/M2/M3 已全部合入 `origin/main`（M2 = PR #46 / `a11b50020`；M3 收尾 = PR #52/#54/#56/#58/#60，2026-08-29 核对 `main` 与 `origin/main` 一致）；M4-M5 远期 |
| §4 全局存量债（CLAUDE.md 迁移） | dompurify、doom_loop 统计、资产路由等                                                                                  | 按到期日跟进                                                                                                                                                  |

---

## 1. 权限档位遗留债（来源：PR #32 `session-permission-tier`）

> 计划：`docs/plan/mode-scoped-permission-overlay.md`（已实施）；验收 §11 全绿，以下为 PR 描述声明的非阻塞遗留。

| 编号 | 负债                                                                                                           | 包            | 风险                                          | 触发/到期条件              |
| ---- | -------------------------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------- | -------------------------- |
| M1   | V2 Runner 系统提示硬编码 override 未激活：Permission Context 不反映当前 break-glass 激活态，模型看不到临时全开 | core          | 模型可能误解自身权限边界，误以为仍受限于档位  | 系统提示重构时             |
| M2   | V1 每 turn 权限快照 vs 60s 租约收权粒度：V1 快照化裁决，收权粒度粗于 V2                                        | aigcfroge     | V1 路径下用户刚收窄权限可能到下一 turn 才生效 | V1 路径再调整时            |
| M3   | break-glass 无持久审计日志：临时全开动作不留审计记录                                                           | core / app    | 越权取证困难                                  | 审计基础设施建设时         |
| M5   | 基线 wildcard deny 在提升路径被丢弃：代码有注释钉死，语义未完整收敛                                            | core          | 未来新增工具可能漏过 deny 语义                | PermissionEffective 演进时 |
| D5   | effect skill 吞错反模式条目拆出：`Effect.catch(() => Effect.void)` / `orDie` / `ignore` 覆盖缺口               | docs（skill） | 分支审查无法引用统一反模式规范                | 下次 skill 更新时          |

---

## 2. 4 模式页面归一化延后项（来源：PR #34 `mode-page-unification-v2.md`）

> 计划：`docs/plan/mode-page-unification-v2.md`（Phase 1-7 待执行，执行提示词 `docs/plan/prompt-mode-page-unification-v3.md`）。
> 以下均为该计划明确排除、需另立专项的项。

| 负债                                            | 说明                                                                                                                     | 触发/到期条件        |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------------------- |
| 实际 `960px` 主列                               | 计划 G2：宽度清理只做"等价才删分支"，不实现 960px；产品要求则另开 PRD/视觉变更，重评窄屏、Assistant 密度、滚动与容器宽度 | 产品提出需求时       |
| Assistant `global\|project` 知识库 scope 选择器 | 由 Assistant 独立计划负责，不在归一化计划实现                                                                            | Assistant 专项启动时 |
| Chat Location 底层 primitive 抽取               | Chat 仍由 `ChatFeatureSidebar` 内联持有 Location + feature tree/counts；是否抽取更低层 primitive 计划明确不强制决定      | 行为等价测试证明后   |
| 全仓 legacy import 债                           | ~249 个 star imports + ~123 个 alias imports（AGENTS.md 已禁止新增，存量不迁移）                                         | 全仓清理专项时       |

---

## 3. Custom Mode 平台任务（来源：PR #33 `custom-mode-docs`）

> 文档：ADR-17 `docs/architecture/adr/ADR-17-custom-mode-composition-platform.md`（Accepted for M0/M1 implementation v1.2，由用户授权 AI 代理代签）、
> PRD `docs/prd/custom-mode-composition-platform.md`（Approved for M0/M1 implementation v1.2）、路线图 `docs/roadmap/custom-mode-roadmap.md`（Approved for M0/M1 implementation v1.2）。

| 阶段 | 名称                      | 核心范围                                                                                 | 关键依赖                 | 状态                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | ------------------------- | ---------------------------------------------------------------------------------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| —    | **ADR-17 正式评审**       | Product/Core/App/Security/Schema+SDK 五方评审与签字                                      | —                        | 已完成（用户授权 AI 代理代签，2026-08-18）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| M0   | 治理与组合底座            | 第五 Mode、Profile/Plan/Snapshot、AssetRef、Resolver                                     | ADR-17 批准              | 已完成（Phase A-F）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| M1   | 单 Agent 可恢复闭环       | `meta` + 一个用户 Agent + Prompt/Skill + native + Upgrade + UI Phase E + 50 轮稳定性矩阵 | M0                       | 已完成（Waves W1-W4，2026-08-19）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| M2   | 多 Agent 与编排           | Agent 池、Command、Workflow、进度、取消、部分成功                                        | M1                       | 已完成并合入 `main`（PR #46，合并提交 `a11b50020`，2026-08-22；R5 独立专项复审已取得并整改，复审 APPROVED，见 [Custom M2 复审报告](review/AigcForge_CUSTOM_M2_REVIEW.md)，遗留项见 §3.1）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| M3   | MCP 与审批                | scoped registration、凭证、健康、统一审批入口（含 once/Session/Location grant model）    | M2 + Tool Registry 扩展  | **Phase A–G 全部交付并已合入 `origin/main`**（收尾 PR #52 `desktop-e2e-fixes` / #54 `route-classification-fix` / #56 `ui-upstream-errors` / #58 `binding-write-validation` / #60 `grant-killswitch-coverage`；Phase G 合并 `4278b45f7` 已是 `main` 祖先；2026-08-29 核对本地 `main` 与 `origin/main` 零差异）。四道 Gate 全过：G3-1（[ADR-19](architecture/adr/ADR-19-mcp-scoped-registration.md) Accepted v1.0）、G3-2（[ADR-20](architecture/adr/ADR-20-scoped-grant-model.md) Accepted v1.2）、G3-3（[ADR-21](architecture/adr/ADR-21-mcp-credential-custody.md) **Accepted v1.2**——v1.0→v1.1→v1.2 的两次修订都不是起草方自查发现，而是执行方交付后由复审方红证抓出，这正是「起草/批准分离 + Slice 0 独立事实复核」这套补偿控制的产出）、G3-4 由 ADR-20 §2.6/§2.7/§2.8 回答。Phase B/D/G 遗留见 §3.2。计划 [custom-mode-m3-mcp-approval.md](plan/custom-mode-m3-mcp-approval.md) |
| M4   | Trusted Runtime Extension | Host/Agent/Client 分面、信任、停止、隔离、回滚                                           | M3 + Plugin 生命周期 ADR | 远期                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| M5   | Code Presentation         | `run_code` + 受限 SDK，共用 Effective Tool Set                                           | M3/M4 稳定               | 远期                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

> 与权限档位的接口约定：Custom M0/M1 必须定义 mode ceiling 与 Snapshot allowlist；应用级审批入口与 grant model 属 Custom M3（权限档位计划明确"不自动批准"）。

### 3.1 Custom M2 遗留项（来源：[Custom M2 复审报告](review/AigcForge_CUSTOM_M2_REVIEW.md) R4 + R5，2026-08-22）

M2 已合入 `main`（PR #46 / `a11b50020`）。以下为判定为「部分闭环」或「已知残留」的项；全部 P0/P1 阻断项的闭环证据见复审报告 §2（R5 的 3 项 P0 与 8 项 P1 见 §2.5）。标注「M3 范围」的两项是 M3 开工 Gate 的输入，见 [M3 计划](plan/custom-mode-m3-mcp-approval.md) §1。

| 负债                                                             | 包               | 说明与根治方向                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Owner | 触发条件                   |
| ---------------------------------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | -------------------------- |
| Custom kill switch 无「关闭即中断在飞 child」的进程内通知        | core / aigcfroge | 已实现：`assertRuntimeSupported()` 对 custom 关时 fail-closed（`product-mode-policy.ts:77`，覆盖 `session.ts` 6 处入口 + `handlers/session.ts:146`）；`WorkflowRunner` 每轮调度前与每个 step dispatch 前各查一次，落 `custom_mode_disabled`（R5-3 已修正该检查用过期 revision 做 CAS 的缺陷）。仍缺：`isCustomModeEnabled()` 只读环境值，无「开关变化」通知入口，关闭开关无法中断已在 Provider 请求中途的 child，最坏多跑到当前 step 结束。根治：加显式进程内 disable 通知，调用 `WorkflowExecution.interrupt` 与 `SessionExecution` 中断并等 finalizer settle 后返回 | TBD   | 运营级 kill switch 实装时  |
| durable handoff 状态未持久化，「已投递 / 已丢失」不可区分        | core             | R5-18 已删除 `WorkflowRunInfo` 上恒缺的 `handoffDigest` / `handoffStatus` / `handoffErrorCategory` 与 `WorkflowHandoffStatus`（契约谎报），但根因未解：`injectSynthetic` 失败只落一条带 `defectTag` 的 `Effect.logError`，run 仍报 `completed`，无持久状态、无重试。根治需要为终态 run 的 handoff 记账在 ADR-18 §2.2「终态不可变」上开一个显式例外，并新增列 + 迁移                                                                                                                                                                                                   | TBD   | 需要 handoff 可观测性时    |
| `MAX_STEPS` 等图不变量不在解码期与资产写入期强制                 | core / schema    | `validateGraph` 唯一非测试调用方是 `composition-resolver.ts:257`，因此 YAML 加载（`workflow-asset.ts loadDir`）与 `workflow-asset/apply`（`propose-workflow-asset.ts validateContent`）会接受超大 step 数组、环、重复 id、悬空 `next`、`branches`+`continue`，资产还会被报成 valid；只有 `freeze` 才拒绝。ADR-18 §2.5.3 写的是「解析期拒绝」，需对齐实现（改代码，不是改 ADR）。另 `Schema.Array(StepDef)` 在三处解码点均无长度上界                                                                                                                                   | TBD   | 资产写入面加固时           |
| `timeoutSeconds` 省略即无超时                                    | schema / core    | `StepDef.timeoutSeconds` 没有 `withDecodingDefaultKey`（相邻 `maxAttempts` / `failurePolicy` 都有），`workflow-runner.ts` 字段缺失时直接 `return yield* delegated`，于是 `failurePolicy: retry` + `maxAttempts: 8` 每次 attempt 都无墙钟上限——比 ADR 宣传的 86400 上限严格更差。加默认值会静默截断合法长任务，属产品决策                                                                                                                                                                                                                                              | TBD   | 需要资源上限硬保证时       |
| `packages/app/e2e` 不在 typecheck 项目内，且带 29 个存量类型错误 | app              | `app/tsconfig.json` 的 `include` 只有 `["src"]`，`e2e/tsconfig.json` 从不被 `tsgo -b` 或 CI 执行。实测 `tsgo --noEmit -p e2e/tsconfig.json` 报 29 个错误，全为存量。这直接放大了 R4 P0-B 的潜伏期——e2e 是当时唯一能发现浏览器白屏的手段，却因门禁缺位而长期不跑。根治：修完 29 个错误后把 e2e 纳入 `tsgo -b` 与 CI                                                                                                                                                                                                                                                    | TBD   | e2e 门禁强化时             |
| Storybook 构建 OOM                                               | storybook        | `bun run build` 在 Vite transform 阶段 OOM，`--max-old-space-size` 4096 与 6144 均崩。移除新增 story 后照样 OOM，属分支既有。后果：Custom M2 的视觉截图门禁无法取得。根治：拆分 stories 入口或降低 preview 构建期内存占用（参考 `manualChunks` 边界既有约束）                                                                                                                                                                                                                                                                                                         | TBD   | 需要视觉回归证据时         |
| `packages/sdk/openapi.json` 长期未随路由再生成                   | sdk              | 该文件由 `script/generate.ts` 第 2 步写出，最后一次更新在品牌迁移期；2026-08-22 实测与实际 spec 差 **72 个 path / 172 个 schema**，绝大多数来自 M2 之前已合并的里程碑。无代码消费方（`packages/sdk/js/script/build.ts` 自己写临时 `openapi.json` 并在结束时删除），故只是发布/参考产物过期，不造成运行时或类型漂移。故意不在 `workflow-surface` 上再生成：会把约 470KB 与 M2 无关的产物混进 M2 diff。注意 `script/generate.ts` 末步会 prettier 格式化全仓，不能整体跑。根治：单独一次提交刷新，并给 CI 加 spec drift 门禁                                             | TBD   | 独立产物刷新提交时         |
| spawn 子进程 / 实例引导测试对机器负载敏感                        | aigcfroge        | `test/project/instance-bootstrap.test.ts`、`test/cli/acp/initialize-auth.test.ts`、`test/cli/acp/skills.test.ts`、`test/cli/run/run-process.test.ts` 在并行跑 lint / typecheck / 子代理时集体 `TimeoutError`（单例 15–59s），空载单独重跑全绿。同类现象还见于 Playwright `session-timeline.spec.ts:33`（全量 59 例失败 1 例，单跑 5 passed）与 exerciser auth（重负载 282/2，空载 284/0）。CI 上表现为间歇失败，容易被误判为回归。根治：为这些文件设更宽的独立超时，或在 CI 中与重负载步骤串行化                                                                      | TBD   | CI 稳定性专项时            |
| exerciser 对 3 个 workflow mutation 端点只覆盖 404 路径          | aigcfroge        | `cancelRun` / `cancelStep` / `retryStep` 在 exerciser 里只注册了「run 不存在 -> 404」场景，没有 200/202 成功路径与 409 stale revision。覆盖门禁按 `METHOD path` 计数，故 `.missing` 场景名同样满足门禁——「已覆盖」不等于「行为已测」。成功路径当前由 core 单测与 Playwright（mock server）覆盖。根治：补三条成功路径与一条 409 场景                                                                                                                                                                                                                                   | TBD   | exerciser 场景扩充时       |
| Custom Builder 资产列表把失败渲染成空态                          | app              | `custom-sidebar.tsx:32-50` 对 5 个资产 list 同时用 per-call `.catch(() => ({data:{assets:[]}}))` 与外层 `catch {}`，任何失败都渲染成与「0 个资产」无法区分的空态，无错误提示也无重试，违反「禁止静默失败」。该模式在 `main` 上对 agents/prompts/skills 已存在，`workflow-surface` 只是扩展到 workflows/commands，按「不顺手修无关代码」未在 M2 范围内修。根治：区分 loading / empty / error 三态并提供重试                                                                                                                                                            | TBD   | Custom Builder 下次改动时  |
| Custom 快照面板与 Builder 的 token / i18n 残留                   | app              | `custom-snapshot-panel.tsx` 新增的 Workflow / Agent Pool 卡片硬编码 Tailwind 调色板（`bg-amber-500/10`、`text-blue-300` 等）并直出英文字面量（`Workflow (...)`、`{n} steps`、`Agent Pool (...)`），违反「颜色走 `--v2-*` token、文案走 i18n」；`workflowStatusKey()` 用服务端状态拼动态 i18n key 且无兜底，超出契约的状态会渲染空 badge 文案。`custom-draft` 的 store 按目录 memo 但 persist 用单一 global key，跨项目会互相覆盖，且 SDK 未就绪时全部落在 `""` key 上                                                                                                 | TBD   | Custom Builder 下次改动时  |
| `createChild` defect 收敛为 `executor_unavailable`，丢失原因分类 | core             | allowlist 拒绝已修为 `agent_not_allowed`（dispatch 前 + 创建后 parent 不匹配），但 `TaskDriver.createChild` 的 defect 仍无差别归 `executor_unavailable`，与「driver 真的缺失」不可区分。当前靠 `defectTag` 日志保留可诊断性；`TaskDriver` 改 root-scoped 后「走错 root」这一主要成因已消除，故降级为诊断质量问题。根治：为 createChild 定义 typed failure 而非 defect                                                                                                                                                                                                 | TBD   | TaskDriver seam 再次改动时 |

| 历史 Snapshot 无 `consumerKey`，Unicode agent 名在新 consumer 校验下 fail closed | core | D5-A/D4-A（S1，2026-08-31）把 `SnapshotDataV2.bindings` 改为 `Schema.optional` 并给 `AgentInfo` 加 `consumerKey`。历史快照没有该字段，`CompositionConsumerView.resolveScope` 回退 `agents/${name}`；若历史 agent 名含空格 / 点 / 非 ASCII，该键无法通过 `ConsumerKey` 正则 `^(orchestrator|agents/[a-zA-Z0-9_-]+)$`，于是被判为 `unresolved` 并 fail closed（此前是静默回退到跨 consumer 扁平数组，属泄漏，故当前行为更安全）。触发条件：Custom Mode 生产激活（`Flag.AIGCFROGE_CUSTOM_MODE` 默认关，故当前不可达）且库中存在 M0-M3 期间以 Unicode 名冻结的 V2 快照。根治：一次性迁移脚本，按 `makeConsumerKey(name, relativePath)` 回填 `AgentInfo.consumerKey` 并同步重命名 `bindings` 键 | TBD | Custom Mode 生产激活前 |

| 增量 lint 只门禁 4 条规则，死 import 与多余断言可静默累积 | script | `script/lint-changed.ts` 的 `guardedRules` 原本只有 `no-unsafe-type-assertion` / `consistent-return` / `unbound-method` / `await-thenable`；其余规则在 `.oxlintrc.json` 里是 `suspicious: warn`，因此 `bun run lint` 与增量 lint 都不会失败。2026-08-31 已把 `no-unnecessary-type-assertion` 与 `no-unused-vars` 加入 `guardedRules`（仅作用于新增行），并即时抓出 `tool/skill.ts` 的一处死 import。剩余部分：其余 warn 级规则仍无门禁，且未对存量代码收敛。根治：逐条评估 warn 级规则是否进 `guardedRules` | TBD | lint 门禁专项时 |

| S4 附件降级只覆盖 data: 与 file:// 的媒体类型，文本/目录/远程 typed 拒绝 | aigcfroge / core | S4（2026-09-01，`b93887df9`；2026-09-01 修订收紧）的 `PromptParts.materializeFilePart` 现在只物化「可以作为 media 下放」的类型：`data:` 原样保留，`file://` 读成 data URL。**`text/plain` 与目录 typed 拒绝**，因为 V1 路径（`createUserMessage` 的 `resolvePart`，`prompt.ts:935-1075`）是把两者经 **Read 工具降为文本上下文**（文本带 LSP 符号范围与 `?start=&end=` 偏移，目录出列表），base64 编码源码不是它的简化版而是另一个错误答案 —— 这是 S4 GREEN 2「V1 与 adapter 共用同一份 mapping」尚未达成的部分：常量共享了，降级逻辑没有。`http(s):` / MCP `resource:` 同样 typed 拒绝。仍缺：把 `resolvePart` 的 file:/directory 降级提取为共享 owner（需要 `ToolRegistry` / `Provider` / `LSP`，且要脱离 `resolvePart` 对 V1 `info.id` 与 `synthetic` 标记的依赖）；PDF 缺 provider 支持矩阵校验（当前一律按 base64 media 下放，对 Anthropic 成立，对不支持 PDF 的 provider 会到 provider 才失败）；MCP blob 大小/MIME 规则常量已共享但 adapter 不走 MCP read 所以未消费。触发条件：客户端向 `prompt_async` 发送文本文件 / 目录 / MCP resource 附件 → 拿到 400（行为已由 `httpapi-prompt-async-v2.test.ts` 第 7 条钉住），或向不支持 PDF 的 provider 发 PDF | TBD | 附件降级提取为共享 owner 时 |

| canonical `session.prompt` 在 admission 既不物化也不拒绝非 `data:` 附件 URI | server / core | 出口侧已经收口：`to-llm-message.ts` 的 `media()` 对非 `data:` URI 降级为显式文本标记（`e8742e257`，行为由 `core/test/session-runner-message.test.ts:157` 钉住），所以 `file://` 再也到不了 provider 的 base64 validator。**未收口的是入口**：`packages/server/src/handlers/session.ts:325-337` 把 `ctx.payload.prompt` 原样透传给 `SessionV2.prompt`，客户端可以 POST `files: [{ uri: "file:///...", mime: "image/png" }]`，它被 durable 存下，直到 provider 阶段才降级成「附件已省略」。用户看到的是自己明确附了文件却被静默省略，而 4xx 本该在提交那一刻就返回。注意计划 §S4 写的是 canonical Prompt 允许携带「明确可延迟解析的 typed URI」，所以正确解法有两条且必须先裁决：**(a)** 在 `session.prompt` 复用 `PromptParts.materializeFilePart` 物化（与 `prompt_async` 同一 owner），或 **(b)** 保留延迟 URI 但在 runner 内实现真正的延迟解析，此时出口守卫要相应放宽。**安全前置**：该端点接受客户端任意 `file://` 路径，任何服务端物化实现都必须先按会话 Location 校验路径，否则等于开放任意文件读取 | TBD | 附件类型扩面或延迟解析实现时 |

| Snapshot Command 只完成冻结保真，runtime / parser / endpoint / App 四条腿未接 | schema / core / server / app | S5（2026-09-01，`b2cdfda6e`）只做了地基：`Composition.CommandInfo` 冻结 `invocation` / `args` / `source`（并删掉无消费者的 `template`），resolver 填充，2 条测试。计划 §S5 其余全部未做：**参数解析**（引号 / Unicode / `$1..$N` / `$ARGUMENTS` / 缺参多参 typed reject / shell fence 不执行）、**durable command admission**（exact retry、409、unbound/global/ambiguous fail closed）、**canonical endpoint**（`packages/server`）、**App**（slash popover 读 Snapshot consumer catalog、immediate 与 queued followup 共用 parser/sender、删 `submit.ts:78`/`:490` 两份 `text.split(" ")` 与 `:95`/`:503` 两份 `images.map(...)`）。此外 `CompositionConsumerView.getCommands`（`consumer-view.ts:93`）**至今零生产调用方** —— Runtime 完全不消费冻结 command catalog，命令仍来自 live `core/src/command.ts` + plugin config，这就是 P1-7「可配置、可冻结、不可运行」的当前状态。触发条件：Custom Mode 生产激活前必须闭合，否则 Builder 里绑定的 Command 全是死配置 | TBD | S5 下半段 |

| `globalThis.AI_SDK_LOG_WARNINGS` 仍以 `@ts-ignore` 挂在 prompt.ts 模块顶层 | aigcfroge | `session/prompt.ts:75-76`。S4 抽取 `prompt-parts.ts` 时按计划 §S4 REFACTOR 的要求**没有**把它迁进新模块，所以它留在原位继续以 `@ts-ignore` 写全局。根治：迁到 typed bootstrap（与其他运行时开关同处），或证明 AI SDK 已提供官方开关后删除 | TBD | LLM 层 bootstrap 整理时 |

### 3.2 Custom M3 Phase B / D / G 遗留（来源：Phase B 与 Phase D 独立复审 2026-08-23，Phase G 覆盖调研 2026-08-27）

Phase B 的 placement 维度与 MCP 命名/冲突 owner 已交付，两项 P1（占用检查 placement-盲、settle 与 definitions 的 placement 未绑定）已在复审中整改并红先行验证。Phase D 的 ScopedGrant owner、grant 咨询、ask TTL、无应答方即时拒绝与 attended 天花板改判已交付，两项 P0（presence 未接线导致全模式 ask 硬拒、grant store 用 `provideMerge` 导出出第二个 Database 实例）已在复审中整改（`6be7d4b7c`）。Phase F0 retention/import preflight 已实现，仍在最终复审。以下为 Phase C/F 输入或刻意不在本轮拍定的债。

| 负债                                                                           | 包                        | 说明与根治方向                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Owner | 触发条件                                                                   |
| ------------------------------------------------------------------------------ | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- | -------------------------------------------------------------------------- |
| MCP health 边表是枚举式的，与「调用期只汇报刚观察到什么」的写法结构性冲突      | core                      | **来源：Custom M3 Phase G，2026-08-27。** 已修一条：`ready → auth-required` 补入合法边（红先行——先把边表测试里该对从非法组移到合法组，转红，再补边；随后行为测试要求过期后 health 为 `auth-required`，再次红→绿）。补它的理由不是「代码声明了意图」，而是**表自相矛盾**：`requestOn` 的同一个 catch 里相隔几行升起 `revoked` 与 `auth-required`，表只给了前者出口。**但同类遗漏不止一条，本轮刻意未改**：实测 `degraded → auth-required`、`auth-required → offline`、`auth-required → degraded`、`offline → auth-required`、`degraded → revoked` 全部非法，而 `requestOn` 的 `tapError` 完全依据「刚观察到的错误」选择目标、不看当前状态，因此这五条都可达（例：token 过期→`auth-required`→用户重新授权→服务器崩溃→请求 `offline`，被拒）。调用方把这些失败 `Effect.catch(() => Effect.void)` 吞掉，于是记录的 health 静默落后于现实。**冲突是结构性的**：表说「进入故障态后只能经 `connecting` 改变状态」，调用期说「把刚发生的事记下来」，枚举边永远调和不了。能调和的规则是「失败观察一律可记录，只有变成 `ready` 才受门禁」——它保留全部与安全相关的非法性（`offline/auth-required/revoked → ready` 仍非法，`revoked → connecting` 仍需 rebind），同时消掉上述噪声类。**顺带结论：在表改成规则之前，不要把这些吞掉的失败改成 error 日志**——本轮曾按此实施并已回退，因为那五条会在正常运行序列上刷 ERROR，比静默更糟。根治：按上述规则重写 `transitionHealth` 并同步边表测试，属独立专项 | TBD   | 需要 health 忠实反映连接状态时（Builder 展示 / resolver `not_ready` 归因） |
| ~~Location 卸载没有可测的生产路径~~ **已移入 §5 已闭环（2026-08-27）**         | core                      | 见 §5 同名条目。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| kill switch 回滚是惰性的：env 翻转到下一次调用之间，连接与工具目录仍挂载       | core                      | **来源：Custom M3 Phase G §7.2，2026-08-27（实测，非推断）。** 实测三阶段：翻转前 `registered/materialized/connections` = true/true/1；**翻转后、下一次调用前仍是 true/true/1**；下一次 `callTool` 被 `McpDisabledError` 拒绝，同一次调用完成拆除，归零。因为开关只在 `connect` / `callTool` 两处读取，没有任何东西在监视这个环境变量。**不是授权洞**——翻转后每一次调用都在派发前被拒；但确实是一个挂载窗口：外部子进程继续运行、stdio 管道继续存在，模型的工具目录仍在广告这些工具，若此后无人调用则一直留到 Location scope 关闭。§7.2 的「回滚不得留半挂载状态」按字面读在这个窗口内不成立。现已由两条 e2e 把该行为钉住（开启路径 / 回滚路径），改成即时拆除会在那里转红。根治：需要 config 变更事件或显式管理端点才能做到即时；若接受惰性，则把「回滚 = 翻 env + 重启」写进运维文档                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | TBD   | 需要不重启即时回滚时                                                       |
| Credential 静态加密（明文存储）—— ADR-21 明确排除在 M3 之外                    | core                      | `credential.value` 是 `text({mode:"json"})` 明文（`credential/sql.ts:9`），`auth.json` / `mcp-auth.json` 同为明文 JSON（但均 `0o600` 落盘）。[ADR-21](architecture/adr/ADR-21-mcp-credential-custody.md) §2.5 裁定：真正的静态加密需先定密钥来源（用户口令 / OS keychain / KMS）、跨平台差异（macOS Keychain / Windows DPAPI / Linux Secret Service）与「忘记口令即丢全部凭据」的产品语义，是独立专项，**不在 G3-3 内假装解决**。M3 只做两项止血：① DB 文件 chmod `0o600` 与既有文件存储对齐（**反直觉现状：秘密最集中的 SQLite 库无任何 chmod，`global.ts:36-42` 只 mkdir**）② `McpServerBinding` 解码期拒绝秘密字面量。根治：加密专项立项                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | TBD   | 企业级/合规要求提出时                                                      |
| 三个 secret 存储并存，「唯一 secret owner」不成立                              | core / aigcfroge          | DB `credential` 表、`auth.json`（provider API keys）、`mcp-auth.json`（MCP OAuth token/clientInfo/codeVerifier/oauthState）各有在役消费方。ADR-19 §2.1 已裁定 V1 MCP 与 canonical 并存不合并、迁移归 M4；[ADR-21](architecture/adr/ADR-21-mcp-credential-custody.md) §2.6 令凭据收敛服从同一裁定——否则 M3 会被拖进 V1 全量消费方迁移。**Phase C 不得为了「统一」去改 `mcp/v2-auth.ts` 或 `auth/index.ts` 的在役语义**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | TBD   | M4 Gate 输入（与 V1→canonical 迁移评估同批）                               |
| `credential.active` 列在 V2 服务零引用                                         | core                      | 该列与 `credential_connector_active_idx` 唯一索引（`migration/20260611035744_credential.ts:21`）来自 V1 连接器时代；`credential.ts` 内 `active` 0 命中。它是一个**撤销形状的列而没有消费方**——ADR-21 §2.6 刻意不动它，避免在决定「采纳还是删除」之前先发明第三套撤销机制（极致减法：复用 → 删除 → 归并 → 重构 → 新增）。根治：确认 V1 面无消费方后删列 + 迁移，或在绑定层撤销之外正式采纳它                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | TBD   | 下次 credential 层清理时                                                   |
| once grant 无签发 Session 归属                                                 | core / app                | F0 已闭环保留策略：签发不再触发清扫；默认 30 天窗口由 Location owner Scope 立即+每小时调用 `prune`，失败分类记录并继续调度；`findValid` 不可见、once 二次消费失败。仍开放：once grant 当前不保存签发 Session，因此 `list({sessionID})` 明确排除 once；Phase F 若需会话级 once 历史，需在人类裁定后选择新增独立归属列+迁移，或保持“未归属”展示。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | TBD   | **Custom M3 Phase F 开工时（与审批中心历史视图同一 slice）**               |
| Agent 资产通配 allow 警示已接入但需持续验证消费链                              | core / app / schema / sdk | 已部分闭环：复用 `PermissionEffective` 真源；propose metadata 与独立 apply `{asset,warnings}` 均由 App 消费；警示不阻断且不改内容。仍需产品/复审确认最终展示位置与真实浏览器矩阵，当前有 Core/schema/metadata 单测、HTTP apply 响应测试与 ChatRightPanel 消费实现；真实浏览器对 apply 成功警示、empty/loading/error、desktop/narrow、light/dark、三语矩阵仍未形成完整行为证据，不能将实现入口记作覆盖。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | TBD   | **Custom M3 Phase F0 复审完成时**                                          |
| MCP 冲突域不是 Location-scoped（`ApplicationTools` 进程全局）                  | core                      | `registry` 的 `local` 闭包按 Location 隔离（`location-layer.ts:267` 以 `Layer.fresh` 收尾，绕过 MemoMap 按 Layer 对象引用的记忆化，已复核），但 `ApplicationTools.layer` 位于 LayerMap `dependencies`（`location-layer.ts:289`）→ 进程全局单实例，而它并入 ADR-19 §2.4 的占用域。后果：Location A 注册的应用工具会占掉 Location B 的同名 MCP 工具名，报 collision。方向是 fail-closed（不会遮蔽、不会跨 Location 泄漏工具），且应用工具本就是 app 级全局概念，故非安全缺陷；但它让 §2.7「跨 Location 隔离」对冲突域不成立，多 Location 并用同一 MCP server 时可能出现无法解释的伪冲突。根治：占用检查区分「Location-scoped 域」与「全局应用工具域」，或把 ApplicationTools 收敛为 per-Location                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | TBD   | 多 Location 并发绑定同一 MCP server 出现伪冲突时                           |
| presence 的粒度比审批 UI 的粒度粗：global SSE 面「有人」≠ 当前路由有审批入口   | app / core                | **来源：合并前审批复核，2026-08-27。** App 只开一条 global 事件流（`context/server-sdk.tsx` 的 `eventSdk.global.event`），`handlers/global.ts:57` 不带 `location` 地 `bindResponder({custom})`，于是**只要 App 连着，任何 Location 任何路由的 presence 都为真**；而审批入口 `ApprovalCenter` 是按路由挂的。原状态下 `/mode/:mode` 与 `/`（HomeOverview）都没挂，落在这两个路由上的 `ask` 既不 fail-closed 也无处应答，而是**静默挂满 TTL（默认 300s）后 `AskExpiredError`**——Custom 模式恰好以 `/mode/custom` 为主入口。**已修一半**：`ModeWorkspace` 现显式传入目录级 accessor 挂载 `ApprovalCenter`（`mode-workspace.tsx`）。**仍开放两点**：① `/` HomeOverview 仍无入口；② mode 工作区的审批范围跟随 `useModeDirectory()` 解析出的单一目录，其他目录的会话审批只在各自路由可见——跨目录聚合视图属产品裁定。根治：把 presence 的绑定粒度与真实 UI 能力对齐（客户端声明「我能应答哪些 Location」），或提供一个真正全局的跨目录审批中心                                                                                                                                                                                                                                                                                                                                                                                                                                                     | TBD   | 审批中心做跨目录聚合时；或产品要求 headless/首页也能应答时                 |
| exerciser 的 `v2.permission.grant.list` 场景恒绿（seed 从不签发 grant）        | aigcfroge                 | **来源：合并前审批复核，2026-08-27。** 场景已注册并满足 `METHOD path` 覆盖门禁，但 seed 从不签发任何 grant，于是断言在「空列表」上恒真——「已覆盖」再一次不等于「行为已测」（与 §3.1 的 workflow mutation 三端点同型）。grant 端点的 409/404/400 负路径目前只有 core 单测覆盖，HTTP 层零负向用例。根治：seed 里先签发一个 grant 再断言投影字段，并补 HTTP 层 409（CAS miss）/404/400 三条                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | TBD   | exerciser 场景扩充时（与 §3.1 同一批）                                     |
| ~~全仓 Prettier 基线漂移（无 CI 门禁）~~ **已移入 §5 已闭环（2026-08-30）**    | 全仓                      | 见 §5 同名条目（与 §4「多文件不符合 Prettier 格式规范」的重复登记合并为一条）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `consultGrant` 不传 `snapshotRevision`，ADR-20 §2.3 的 revision 收窄只落地一半 | core                      | **来源：合并前审批复核，2026-08-27。** grant 的 scope 可携带 snapshot revision 以把授权钉在某个组合版本上，但 `PermissionV2.consultGrant` 当前不传该字段，于是收窄条件在咨询侧不生效（方向是**更严会变成更松**：本应因 revision 不符而失效的 grant 仍可命中）。当前实际影响有限，因为 grant 本身按 Location/Session 与 action/resource 收窄且 leaf `assert` 仍是最终边界。根治：consult 时带上当前 Snapshot revision，并为「revision 变更后旧 grant 失效」写红证                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | TBD   | 组合版本变更需使旧授权失效时                                               |

---

## 4. 全局存量债（迁移自 CLAUDE.md 债表）

| 负债                                                                                                      | 包                     | 风险                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Owner  | 到期日                                                                  |
| --------------------------------------------------------------------------------------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------- |
| `.agent.md` handoff frontmatter 的 `model` 字段被 file-loader 静默丢弃（`handoffs[].model`）              | core                   | **来源：S3（D13 handoff，2026-09-01）。** `file-loader.ts` 的 handoff filter 只校验 label/agent/prompt 并保留 `send`；`model` 与模型选择 owner 纠缠（agent-asset `config.model` / variant / 模型选择器），本轮刻意不做。触发条件：任何 `.agent.md` 声明 `handoffs: - {label, agent, prompt, model}` 时 model 不生效且无提示。根治：为 handoff 的 model 字段定义解析与选择语义（S6 Builder handoff 编辑器或模型选择器联动）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | TBD    | 处理 handoff 编辑器时                                                   |
| @ai-sdk/google patch 未上游化                                                                             | root patches/          | 功能补丁可能滞后                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | TBD    | 上游监控                                                                |
| ~~`bun turbo test` 只跑 5 个包，6 个包共 775 个用例在 CI 从不执行~~ **已移入 §5 已闭环（2026-08-30）**    | root turbo.json        | 见 §5 同名条目。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| KB 写路径与 write/edit 工具不共享互斥锁                                                                   | core                   | `session/kb-service.ts` 自带一把 `KeyedMutex`（S4 落地）序列化 KB 自己的「查重 + 写」，但 `FileMutation` 的锁是内部变量（`file-mutation.ts:92`），没有出现在 `Service.of({ create, write, writeTextPreservingBom, writeIfUnchanged, remove, writeAtomic })` 上。因此 `write`/`edit`/`apply-patch` 对同一个 `.md` 的写只走 FileMutation 内部锁，与 KB 的临界区互不排斥：KB 查到「文件不存在」→ write 工具建了它 → KB 的 `writeAtomic` 覆盖掉。窗口窄（`kb_*` 对默认 agent 与 general 已 deny，write 需审批）。根治要给 FileMutation 加一个「在锁内跑调用方前置检查」的组合子——**不能**直接把 `writeAtomic` 包进外层同键锁，KeyedMutex 非重入会死锁                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | TBD    | 下次动 FileMutation 表面时                                              |
| KB 收敛 sweep 只覆盖 global 作用域                                                                        | core                   | `KBService.startupLayer`（S4 落地，已 `forkIn(scope)` 不再阻塞启动）只扫 `Global.Path.config/knowledge-base`（2026-08-30 审查修正：此前误扫 `Global.Path.data`，与全部写入路径使用的 `Global.Path.config` 不一致——sweep 空转，且若 data 目录意外存在 `.md` 会把 global 索引误删；已修复并补 it.live 回归测试）。project 作用域的镜像在 `<directory>/.aigcfroge/knowledge-base` 下，启动时不存在「当前项目」概念（目录由每个 Location 决定），所以 project 侧收敛得挂在 Location 建立时。注意 S4 的三条 KB 原子性测试用的全是 `scope: "project"`——测试覆盖的作用域恰好没有兜底                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | TBD    | Location 生命周期挂钩时                                                 |
| 凭据正则第三份平行实现                                                                                    | http-recorder          | `packages/http-recorder/src/redaction.ts:30` 自带 `SECRET_PATTERNS: ReadonlyArray<{label, pattern}>`，与 `schema/src/credential-scan.ts` 的 `SECRET_PATTERN_LIST` 单一真源并存。形状与用途不同（cassette 的 env/头脱敏需要 label 做标注，不需要 `type` 联合），可以判定为独立 owner；但 S1 刚因为 core 抄了一份而漏掉两类形态（`e4a036866` 只改 schema），第三份存在同样的漂移路径。归并前至少要在两边互相指名                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | TBD    | 下次给 cassette 加脱敏模式时                                            |
| markdown 正文允许远程 `<img>` 与 CSS `url()`（信标外泄面）                                                | session-ui             | S1 决定**保留**图片渲染：服务端 CSP 刻意允许 `img-src 'self' data: https:`（`aigcfroge/src/server/shared/ui.ts`），`timeline-playground.stories.tsx:263` 也用 `![Alt text](…)` 作 fixture。所以模型输出/工具输出里的远程图片 URL 可作信标（"这条消息被看过了"+近似 IP）。收窄属 CSP 决定（`img-src 'self' data:`），不在 sanitizer 层用禁标签解决 —— 禁标签会连 `data:`/本地图片和 `alt` 一起删掉                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | TBD    | 决定 CSP `img-src` 收窄时                                               |
| sanitize 属性层 hook 不覆盖 MathML/SVG 元素                                                               | session-ui             | **来源：docs-v2-baseline 分支 PR 审查，2026-08-30。** `markdown-cache.tsx` 的 `afterSanitizeAttributes` hook 以 `node instanceof HTMLElement` 早退，跳过 sanitize config 显式放行的 MathML（`USE_PROFILES.mathMl`）与 SVG（`ADD_TAGS: ["svg","path"]`）元素：`<math style="position:fixed;inset:0">` 这类 payload 不会被摘掉出流定位。结构性兜底 `contain: layout`（markdown.css）使容器成为 absolute/fixed 后代的定位包含块并建立层叠上下文，逃逸不出 markdown 容器——残余风险为容器内遮罩。根治：hook 对 MathML 元素剥离整条 inline style（KaTeX 语义层不依赖 inline style），SVG 按 mermaid 兼容性评估后决定                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | TBD    | 下次动 sanitizer 配置时                                                 |
| dompurify 锁定 3.4.6                                                                                      | session-ui             | 残留 moderate advisory（IN_PLACE/setConfig/hook 污染类，本仓静态配置+单 hook 用法不可达）；≥3.4.7 与 happy-dom 探针环境不兼容（p/a/svg 被误剥、foreignObject 误放），升级前须先迁移探针到真实浏览器环境                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | TBD    | 2026-08-27                                                              |
| 工具活动 doom_loop 拦截统计依赖 runner 错误文案匹配（"blocked by doom_loop approval"）                    | app                    | `session/runner/llm.ts` 文案变更会静默漏计；且只覆盖 denied/rejected，CorrectedError 反馈不计入。根治：事件层为 tool error 加结构化标记（如 `cause: "doom-loop"`），UI 按字段判断                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | TBD    | 事件层加标记时                                                          |
| 工具活动统计随会话压缩缩水                                                                                | app                    | 统计基于消息 parts，compaction 重写历史后旧 part 被丢弃，计数仅反映当前上下文窗口。根治：event/DB 层聚合持久统计，UI 只读                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | TBD    | 需要持久指标时                                                          |
| workflow/plugin 未建 typed service 而是 handler 内联写事务                                                | aigcfroge              | 虽已复用 FileMutation 与 KeyedMutex 恢复 5 大不变量，但未在 core 层封装为标准 Service                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | TBD    | 统一资产服务重构时                                                      |
| chat 模式下 repeat-detection 启发式分词与语言支持不完备                                                   | app                    | 采用混合分词与单 token 旁路，长文本混合场景可能存在边界漂移                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | TBD    | 意图识别升级时                                                          |
| Import-parser 多候选同名时依赖后缀 disambiguation                                                         | core                   | 导入包含多个同名未命名代码块时生成序号后缀，需依赖后续用户在 UI 侧重命名                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | TBD    | 导入流增强时                                                            |
| 资产 apply/delete 缺非会话路由，工作台伪造 sessionID                                                      | aigcfroge / app        | 路由为 `/session/:sessionID/<kind>-asset/...`，模式首页无会话上下文，前端填 `"ses-home-delete"`；`SessionID` 只校验 `startsWith("ses")` 故静默通过，审计归属链断裂（PRD §8.3.1 已声明 sessionID 非写边界前提，故非安全缺陷）。范围与决定见 [Chat PRD §20.6](../docs/prd/chat-mode-creation-layer.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | TBD    | 下次资产端点改动时                                                      |
| P1-10: `resolveSecurePath` 零调用者死代码                                                                 | core                   | `fs-util.ts:257` 的 `resolveSecurePath(worktree, target)` 全仓无调用者（2026-08-14 审计点名），是 fs 工具层的历史残留；可能误导后续路径安全实现去复用一个未经验证的封装。根治：确认无消费方后删除，或纳入统一路径安全封装                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | TBD    | 下次 fs 层清理时                                                        |
| `Effect.gen` 内直接用 `Date.now()`（grant/mcp 共 12 处）                                                  | core                   | **来源：Custom M3 合并前整改复核，2026-08-27。** `AGENTS.md` §Effect Coding 要求 `Effect.gen` 内取当前时间用 `DateTime.nowAsDate`（走 Effect Clock，可被 TestClock 驱动）。`grant/store.ts` 与 `mcp/*` 有 12 处直接 `Date.now()`。**为何本次不改**：这些站点分两类——① 行时间戳与 `issued_at`/`timeUpdated` 记账，不参与任何判定；② `store.ts:381` 的 `isExpired(row, Date.now())` 参与判定，但现有测试是用 `expiresAt: Date.now() - 1` 这类**绝对过去/未来时间戳**做确定性驱动的，而 `TestClock` 从 epoch 0 起算，一旦改成 `DateTime.nowAsDate`，这些种子值的相对关系全部翻转，需要连带重写过期用例；剪枝调度本身已经走 Effect Clock（`TestClock.adjust` 有效）。即测试可控性已由「可注入的 `input.now` + 绝对种子值」满足，本条是风格偏离而非功能缺陷。**按 CLAUDE.md 方案对冲显式申报**：选择了不churn的简单实现。根治：连同过期用例一起迁到 `DateTime.nowAsDate` + `TestClock`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | TBD    | 下次需要用 TestClock 驱动 grant 过期语义时                              |
| 存量 `catch (e: any)` 3 处                                                                                | core / aigcfroge       | main 既有（非分支新增，不违反 No Cheating 新增门禁）：`fs-util.ts:234` 用 `e?.code === "ENOENT"`（ErrnoException）；`session/llm.ts:143` 用 `e.message ?? String(e)`（LLM SDK 可能抛带 `.message` 的普通对象，`instanceof Error` 改写会变 `[object Object]`，须保留 `.message` 访问语义）；`cli/cmd/github.handler.ts:631` 本体已内部 instanceof 收窄，近乎免费。根治：逐 site 核对语义后改 `instanceof Error` + 类型守卫                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | TBD    | 下次各自模块清理时                                                      |
| 包住 `<main>` 的全局 `<Suspense>` 无 fallback，任一 pending resource 都会把整个主区域渲染成空白           | app                    | **来源：Custom M3 Phase F4b 复核，2026-08-26。** `pages/layout.tsx:43` 是 `<Suspense>{props.children}</Suspense>`，**没有 fallback**；全应用仅三处 Suspense（`layout.tsx`、`components/status-popover.tsx`、`components/chat/asset-workbench.tsx`），所以 `<main>` 下任何组件读到 pending resource 都会向上抛到这里并渲染成空。实测后果有三层：① Custom Builder 首次加载 plan 期间整页空白（Playwright 页面快照只剩根节点）；② `Recalculate` 会把整列卸载，于是 `McpTab` 自己的 loading 分支永不渲染——一段被 i18n 覆盖、被 `mcpPreviewState` 显式建模的代码是不可达的；③ 任何后续在 `<main>` 下引入 resource 的页面都会重现同一形态，且症状（空白）与原因（Suspense）距离很远，很难归因。F4b 已在 `custom-preview-column.tsx` 用 `planResult.latest` 做**局部**隔离并补了 e2e 红证，但其余页面未处置。根治：给 `layout.tsx:43` 加真 fallback（需设计输入，属 `DESIGN.md` 范畴），并回归各页面加载观感                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | TBD    | 做加载态设计专项时；在 `<main>` 下新增 resource 之前                    |
| E2E 矩阵门禁在 `docs/testing.md` §4 有声明但全仓从未实现                                                  | app                    | **来源：Custom M3 Phase F4 复核，2026-08-26。** §4 原写「必须覆盖：桌面与窄视口、light/dark、en/zh/zht 三语、键盘 focus、加载/空/错误态」，实测 18 个 `e2e/regression/*.spec.ts`：**dark 0/18、i18n（zh/zht）0/18、keyboard 2/18**，viewport 仅 10 个文件各有 1-2 处 `setViewportSize`（多为设桌面尺寸而非测窄视口）；配置层也无兜底——`packages/app/playwright.config.ts:43` 只有单个 `chromium` / Desktop Chrome project，无 theme / locale / 窄视口 project。**风险是双向的**：一是明暗、三语、键盘可达性回归无任何拦截；二是这条纸面「必须」会被用来卡个别 PR，而全仓中位数从未达标，造成不一致执法。§4 措辞已同步改为「当前实际执行标准 / 目标」两段。根治：在 config 加 theme / locale / 窄视口 project（一次加上会照亮全部 18 个既有 spec，需配套修复预算），或为矩阵另立可执行门禁                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | TBD    | 做 e2e 矩阵专项时；在把 §4 当作 PR blocker 之前                         |
| 浏览器 auto-accept 的 Location 通配键在 V1 面仍是无过期/无撤销/审计不可区分的自动应答器                   | app / core             | **来源：Custom M3 Phase F5 复核，2026-08-27。** 先说不成立的部分：`context/permission.tsx` 的自动应答只对 V1 `permission.asked` 生效，而 V2 ask 是 `permission.v2.asked`，所以 **MCP 与 custom 审批在结构上不可能被浏览器自动放行**。这一点原先只是事件名巧合（两种 ask 经 `handlers/event.ts:58` 把 `data` 重命名为 `properties` 后在回调处形状同构，事件名字符串是唯一边界，且无任何测试），F5 已把它固化为 `autoRespondableAsk` 的返回类型：放宽守卫接纳 V2 会 typecheck 失败（V2 `properties` 缺 `permission`/`patterns`/`always`）且单测转红，二者均已红证。**仍然开放的是 V1 面**：`handlers/session.ts:808-815` 让同步 `prompt`/`command`/`shell` 即使 `AIGCFROGE_V2_RUNTIME=true` 也留在 V1 路径（缺 `Admitted→WithParts` 适配器），因此非 custom 模式下 `base64(directory)/*` 通配键叠加 `parentID` 血缘继承，会自动应答该 Location 内所有会话的 ask；键存 localStorage（`Persist.serverGlobal` → `utils/persist.ts`），无过期、无撤销记录，且回放的 `permission.replied` 不带 actor 字段，与真人点击逐字节相同——审计无法区分「有人批准」与「无人在场」。**它只回 `once`、从不回 `always`**，故不产生服务端 grant：是自动点击器而非授权源，这正是 ADR-20 把 scoped 授权收在 `ScopedGrant`（过期/撤销/审计）的理由。**需要产品裁定**，三个方向的用户可感知代价不同：(a) 删除通配键——「本目录一直允许」体验消失，很可能被当作回归；(b) 给通配键加 TTL——开着的标签页过一段时间会重新询问；(c) 随 V1 退役迁成 `ScopedGrant level:"location"`——语义最正确，但通配键映射到哪个 scope、血缘继承是否保留都是新的产品决策。倾向 (c) 并与 V1 退役同批 | TBD    | V1 同步 prompt/command/shell 退役时；或产品对 (a)/(b)/(c) 表态时        |
| 历史计划/提示词文档仍写着 `bun --cwd <pkg> run <script>`，该形式静默无操作且 exit 0                       | 全仓 docs              | **来源：Custom M3 Phase F5 复核旁路发现，2026-08-27。** bun 1.3.14 实测：`bun --cwd <pkg> run <script>` 打印 `bun run` usage、**不执行任何脚本、退出码 0**；`bun --cwd <pkg> <script>` 与 `cd packages/<name> && bun run <script>` 均正常。危害是「绿了但没跑」——照文档敲命令的人和 agent 会拿到一个空的成功。**CI 不受影响**（`test.yml:145`、`storybook.yml:40` 用的是无 `run` 的正确形式，其余走 turbo），所以门禁本身有效。已修：`docs/testing.md`（§0 加根因说明 + 7 处命令）、`prompt-custom-mode-m3-mcp-approval.md`（把原先只针对迁移脚本的窄提示改成通用规则）。**未修**：54 处散落在 17 份已完成里程碑的 plan/prompt 文档（M0/M1/M2、mode-page-unification、todo-task 等，2026-08-27 实测；计数排除 `docs/testing.md` 与本文件里作为反例引用的那两处），改动量大而收益低，故只登记不清扫。**这条本身是一次不收敛的例子**：该缺陷 2026-08 就在 M3 提示词里被记过一次，但只记成了「迁移脚本的调用形式」，没有上升为「`--cwd` 不能带 `run`」，于是复现。根治：清扫剩余文档，或在 `script/` 加一个 lint 规则拦截该形式                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | TBD    | 下次文档批量清理时；或再有人被同一形式坑到时                            |
| ~~`test/server/` 硬门禁里有一条依赖 github.com 的用例，网络不通即红~~ **已移入 §5 已闭环（2026-08-30）**  | aigcfroge              | 见 §5 同名条目。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `SessionExecution.setBusySeamForTesting` 全局测试 seam 位于生产模块                                       | core                   | `session/execution.ts` 模块级可变状态，`execution/local.ts` 真实 `isActive` 每次调用都经过该 seam；仅测试可设置且有 finalizer 复位，但生产代码路径携带测试后门，误用会让 busy 判定说谎。根治：实例 HttpApi 测试装配（`HttpApiApp.routes`）暴露 SessionExecution 注入点，或 busy 场景改用真实 drain 构造（挂起 LLM stub + busy 信号轮询）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | TBD    | 测试装配层改造时                                                        |
| 未匹配的 API 路径被 Web UI 兜底接走，返回 SPA 而不是 404                                                  | aigcfroge              | **来源：桌面端 e2e 复核延伸排查，2026-08-28。** Effect `4.0.0-beta.83` **自带** 404：`HttpServerError.RouteNotFound` 实现 `Respondable` 并返回 `Response.empty({status:404})`。但 `routes/instance/httpapi/server.ts` 的 `uiRoute` 注册了 `("*", "/*")` 全域兜底（`HttpRouter` 的 `addAll` 会展开成 `/*` 与 `""` 两条、覆盖所有方法），于是 `RouteNotFound` 在本仓**不可达**：任何未知路径、以及对已声明路径的错误方法请求，都落到 UI 兜底并拿到 SPA 200。对照物是 `packages/server/src/routes.ts`——它没有兜底，未知路径本来就 404。本批只修了同一条路径上的另一半（上游不可达时的空 500 → 502 带体），**没有**收窄兜底，因为选择判据无法安全推导：(a) 按 API 前缀白名单收窄（`/api/`、`/global/`、`/tui/`、`/custom-profile/` …）会随新增前缀漂移，漏一个就静默回到 SPA 200；(b) 在服务端镜像 `app.tsx` 的 5 条 SPA 路由形状，则 App 改路由时 deep link 直接 404，失败模式比错误状态码更糟；(c) 按 `Accept` 做内容协商——**已实测排除**：生成的 SDK（`packages/sdk/js/src/v2/gen`）根本不发 `Accept` 头，规则对主要客户端不生效。另需注意两条错误的修法：全局 `catchAll → 404` 会把 `fenceLayer` 的 DB 失败与 SSE 失败一起变成 404（`test/server/httpapi-workspace-routing.test.ts` 已钉了一条 matched-but-erroring 的 500）；放宽 `middleware/error.ts` 去接 `Fail` 会让已声明的 typed 错误契约塌成通用 500。根治：需产品裁定「未知 `/api/*` 应当 404 还是继续返回 SPA」，再在 `serveUIEffect` 入口按裁定加一条判据并补 e2e deep-link 回归                                                                                                          | TBD    | 产品对未知 API 路径的状态码表态时；或 SDK 开始依赖 404 区分路由不存在时 |
| HttpApi exerciser 的 coverage 门禁曾只验「场景已注册」不验行为；残余：effect 模式仍挂 `continue-on-error` | aigcfroge / core       | **更新（2026-08-30，S5）：coverage 半边已闭环** —— `routing.ts` 的 `coverageResult` 已删，coverage 模式只报注册覆盖（COVERS/TODO/MISS 计数），不再出现 pass/fail 词汇，行为断言专属 `--mode effect`；2026-08-28 三条既有失败中 `agent-asset.apply`（ApplyResult 声明契约，断言按响应形状归一）与 `session.task.get`（`ScheduledJob.recoverStaleClaims` 设计语义会把无 schedule 的 in_progress 重置为 pending，seed 改 pending）已修并转绿。**残余开放**：`session.workflow.run` 的 500 根因超出 exerciser（EventV2 跨连接写 deadlock，见本节同名条目），按 S5 规则停下报告，`test.yml` effect 模式的 `continue-on-error: true` 未摘；在该核心修复落地前，必须被硬门禁保护的 HTTP 行为断言仍应写进 `test/server/*.test.ts`（`httpapi-layer.ts` 的 `requestInDirectory` 真起服务）而不是只放进 exerciser                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | TBD    | EventV2 跨连接 deadlock 修复并摘掉 effect continue-on-error 时          |
| 凭据模式在 http-recorder 还有第三份平行副本                                                               | http-recorder / schema | **来源：S1 审批，2026-08-29。** S1 已把 `core/src/credential-scanner.ts` 的本地 `PATTERNS` 删掉、改遍历 `schema/src/credential-scan.ts` 的 `SECRET_PATTERN_LIST` 单一真源（`SECRET_TYPES → SECRET_PATTERN_LIST → SECRET_PATTERNS` 派生链，core 的 `Hit["type"]` 取自派生类型，结构上不可能再漂移）。但 `http-recorder/src/redaction.ts:30` 还有一份自己的 `SECRET_PATTERNS: ReadonlyArray<{label, pattern}>`，形状（带 label 供 cassette 标注）与用途（录制期扫 `process.env` 与请求头）都不同，所以**本次刻意不合并**——强行归一会把 cassette 的 label 语义塞进 admission 用的真源。风险是同一件事仍有两处黑名单：给 schema 加了新形态而没同步 http-recorder，录制的 cassette 就会漏脱敏。根治：抽一层「模式 + 元数据」的共享清单，http-recorder 在其上叠 label                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | TBD    | 下次向 `SECRET_PATTERNS` 增加形态时                                     |
| markdown 里的远程资源仍是未门控的外泄信道                                                                 | session-ui / aigcfroge | **来源：S1 审批，2026-08-29。** 不可信 markdown（模型输出）里的 `![](https://…)` 与内联 `style` 的 `url(https://…)` 都会在渲染时自动发出请求，等于一条不经权限审批的静默出网通道。**刻意不在 sanitizer 层解决**：服务端 CSP 明确写了 `img-src 'self' data: https:`（`aigcfroge/src/server/shared/ui.ts`），`timeline-playground.stories.tsx:263` 也把 `![Alt text](…)` 当渲染 fixture，禁 `<img>` 标签会删掉一个在用功能且丢掉 alt 文本（S1 初版这么做过，已回退）。根治是收窄 CSP `img-src`（去掉 `https:`，改由服务端代理转发），属独立决定：要先想清楚离线/代理场景与 KB 笔记里的外链图片                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | TBD    | CSP 收窄专项启动时                                                      |
| happy-dom 上的 sanitize 断言只在 payload 位于文档首位时可信                                               | session-ui             | **来源：S1 审批实测，2026-08-29。** DOMPurify 依赖 live `NodeIterator`，happy-dom 的 iterator 在当前节点被 `removeChild` 后失效——只要 payload 前面有任何元素被删除（`<unknowntag></unknowntag>` 就够），它之后的节点**完全跳过属性消毒**：实测 `onclick`、`javascript:` href、`style`、`<form action>`、`<img>` 全部原样存活；真实 Chromium 全部正确消毒。所以 `sanitize-regression.test.tsx` 的绿取决于 payload 位置而非 config 是否生效，它**测不出 sanitizer 回归**。已缓解：真实浏览器证据落在 `packages/app/e2e/regression/markdown-sanitize.spec.ts`（真实几何 + 反向验证过会红），单测文件头部写了这个缺口。根治要么等 happy-dom 修 iterator，要么把消毒断言整体迁到浏览器测试                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | TBD    | happy-dom 升级时复测 iterator 行为                                      |
| Builder 的「custom 已停用」判定依赖服务端英文文案子串匹配                                                 | app / aigcfroge        | **来源：kill switch OFF 覆盖补测，2026-08-28。** `custom-preview-column.tsx` 的 `classifyPlanFailure` 用 `msg.includes("Custom mode is disabled")` 认出停用态；`disabled` 是唯一把 Builder 从红色错误降级为琥珀色「去开开关」提示的信号，也是 `canStart` 的两个否决位之一，判错就会让 Start 对着一台必定拒绝的服务端变成可点。服务端那侧是 `ProductModePolicy.CUSTOM_MODE_DISABLED_MESSAGE`，而 `InvalidRequestError.kind` 这个可选字段**本身已在别处启用**（`handlers/session.ts:502/512` 写 `"permission-override"`、`middleware/workspace-routing.ts:199` 写 `"Query"`、`middleware/schema-error.ts:31` 透传 `error.kind`），只是 custom 停用分支的 4 处构造（`handlers/custom-composition.ts:27/40/89`、`packages/server/src/handlers/session.ts:191`）全都只传 `message`——也就是说结构化信号是**已有机制未被采用**，不是缺失。所以这条判定对服务端改文案和 i18n 都不耐受：文案一变，`disabled` 静默变 undefined。本批已把它从内联分支提成导出的纯函数并钉住 6 个用例（含一条「本地常量必须包含该 marker」的漂移哨兵），但**没有**改协议——常量在 app 侧是刻意复制的字面量，不能从 core 引入（`app` 引任何传递依赖 `core/flag/flag.ts` 的模块会让 Web 构建白屏）。根治：服务端在停用分支写入 `kind: "custom_mode_disabled"`，客户端改判 `kind`，字符串匹配降级为兼容旧服务端的兜底                                                                                                                                                                                                                                                                | TBD    | 该文案被改写或进入 i18n 时；或 `InvalidRequestError.kind` 首次被写入时  |
| EventV2.publish 跨连接写 deadlock：workflow 持久写 500，exerciser 7 个场景受阻                            | core / server          | **来源：S5 exerciser 诊断，2026-08-30。** `session.workflow.run` 期望 202 实得 500：EventV2.publish 在连接 c2 开 `begin immediate`，其 commit 回调经 WorkflowRun 的 db（连接 c1）写 `workflow_run`——c1 等 c2 的写锁、c2 等回调完成，busy_timeout 5000ms 后 SQLITE_BUSY→rollback→500。成因：同一路径建出多个 sqlite client（`Database.defaultLayer` 与 `Database.node = LayerNode.make(layerFromPath(path()))` 是两个不同 layer 对象，memoMap 不去重；LayerMap 惰性构建 location 层时 Database 重新建连，EventV2.Service 漏到 app 图实例）。旁证：`packages/core/src/workflow/event.ts:42` 的 `commit: (seq) =>` 丢了 tx 参数（对照 `grant/event.ts:36` 的 `commit: (seq, tx) =>`）；`packages/core/test/workflow-run.test.ts` 16/16 全挂（纯 core 层复现，与 exerciser 无关）。S5 新增的 6 个 workflow 场景（cancelRun/cancelStep/retryStep 的成功与冲突路径）seed 即死于同一把锁，断言从未真跑过，须在本条修复后复验。根治：EventV2 与 WorkflowRun 共用同一连接/事务边界（commit 回调走同一 tx），随后摘掉 effect 模式的 continue-on-error                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | TBD    | 核心修复落地、摘 effect continue-on-error 时                            |
| S2 kernel 的跨连接守卫测试未交付                                                                          | core                   | **来源：S2 已实施（`54f665d5a` 内核 + `1aa5434d4` `EventV2.publishBatch` + `9dd4851d6` 两个入口 + `d4bc786dd` 客户端一次调用）。** 已交付：`admitWithSelection` 先校验后提交、`publishBatch` 单事务提交 N 个 durable 事件且通知全部延到提交之后、canonical `session.prompt` 与 legacy `prompt_async` 都一次携带 selection、app/TUI handoff 退掉两段 dance。**未交付的是探针 P4 那条守卫**：生产装配「全图同一个 `Database.defaultLayer` 对象 → 单连接」目前仍是隐式保证，没有测试阻止将来有人建第二条指向同一文件的连接（探针已证跨连接必死 `SQLiteError: database is locked`，且该失败在压力下才偶发，最难查）。根治：复用探针 P4 的 temp-table marker 手法，断言 kernel 的写入与 EventV2 的写入落在同一连接上                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | TBD    | 下次动 Database layer 装配时                                            |
| handoff `always` 的 tui 持久化接线缺失（app 已落地）                                                      | tui                    | **来源：T4 切片，2026-09-02；app 侧已落地（`09f3ff64e`）。** `planHandoff` 已接受 Location 限定的授权集（`handoffAuthorizationKey(location, label, targetAgent)`，`de1c9b61b`，5 条新测试钉住：命中直接 `switch-*`、异 Location/异 label 不授权、授权计划执行时零 `confirm`），11 条受保护 handoff 用例未动。App 已接线：确认对话框加 remember checkbox（`handoff-confirm.tsx`），确认+记住时把 `handoffAuthorizationKey` 写入 workspace-scoped `Persist` store（`handoff-grants`，per-directory 不跨项目），`handoff()` 读授权集喂给 `planHandoff`（session.tsx）。**tui 未接线**：`subagent-footer.tsx` 的 `handleHandoff` 仍不喂 `authorized`/`location`/`label`，`DialogConfirm.show` 无 remember 选项，且 tui 的确认后持久化（tui 有 localStorage 设施：`context/local.tsx`/`kv.tsx`）未实现。触发条件：tui handoff always 落地时                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | TBD    | tui handoff always 落地时                                               |
| S5 canonical Command endpoint 未落在 packages/server                                                      | server                 | **已闭环（2026-09-02）：`fd515c713` 端点 + `833a10246` spec 再生成。** 见 §5 同名条目。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 已闭环 | 2026-09-02                                                              |

---

## 5. 已闭环

| 负债                                                                                           | 闭环日期   | 闭环提交/分支                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Chat 模式下 meta 默认权限依赖前置拦截（fail-open 信封）                                        | 2026-08-16 | `session-permission-tier`（meta V1/V2 基线 fail-closed + `PermissionEffective`）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| meta 非 coding 模式委派 build 死路                                                             | 2026-08-16 | `session-permission-tier`（Phase 5）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Custom M0 de-scope：`createCompositionSkillCatalog` seam 无生产 caller                         | 2026-08-19 | `custom-rollout`（M1 Runner 接线消费：skill tool lookup 与 skill steer 均走 Snapshot-local catalog，缺行/解码失败/漂移 fail-closed）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `TaskDriver.active()` 用进程全局「最后写入者胜」选实现，可跨 composition root 误选             | 2026-08-22 | `workflow-surface`（已合入 `origin/main`，2026-08-29 核对）：`tool/task-driver.ts` 删除进程级注册栈，改 `Context.Reference` `Runtime` + 私有 `RuntimeState` Ref，`active()` 只解析当前 Context，缺失即 `Effect.die` fail-closed；`installForTesting` 仅返回值、须由测试自行 `provide`，不再改全局选择。证据：`packages/core/test/task-driver-fill.test.ts:622` "isolates simultaneous composition roots through the runtime context"、`:648` "fails closed when no composition root runtime is provided"                                                                                                                                                                                                                                                                                                                                                                                                       |
| Custom 委派 child 在真实 provider turn 上不可用（R6-0 P0）及拟修复自身三缺陷（R6-1/R6-2/R6-3） | 2026-08-23 | 已合入 `main`（合并提交 `b9c6d1077`，复审通过：复审方以纯函数探针独立复跑确认三项翻转，合并后 core 全量 2061 pass / 2 skip / 0 fail）：`runner/llm.ts` per-turn 主 agent 门禁豁免收窄为 `mode==="custom"` child（create 期 assertAgentAllowed + 派发期 allowlist 双门禁为依据）；`permission/effective.ts` unattended custom 天花板改只读白名单（glob/grep/list_assets/read，ADR-20 §2.6）且 base 显式非通配 deny 保留并排在白名单之后。证据：`test/custom-child-provider-turn.test.ts` "non-meta custom child completes one real provider turn without dying"、"non-custom children stay subject to the per-turn primary gate (R6-3)"；`test/permission-effective.test.ts` R6 整改块 3 例（read .env deny 与 coding 配对 / task_spawn+webfetch deny / 未收录 action 默认 deny）                                                                                                                               |
| attended custom 资产自授权限绕过审批（§3.1 R6 残留的 attended 半边）                           | 2026-08-23 | 分支 `scoped-grants`（已合入 `origin/main`，2026-08-29 以 `packages/core/test/permission-ask-bounds.test.ts` 存在于 `main` 核实）：产品裁定重写目标为 **`ask`** 而非 deny（ADR-20 §2.6，与 2026-08-16 meta 裁决同型），`effective.ts` 对 `mode === "custom"` 的非白名单资产来源 allow 改判 ask 并保证位序（改判 ask 前置 → 白名单 allow → 显式非通配 deny），saved 追加来源不受影响；`llm.ts` 每轮 provider turn 校验 agent 注册表条目的 `originRelativePath`+`originRevision` 与绑定资产一致，否则 `AgentProvenanceError` fail closed（堵同名冒名变体）。配套落地 ADR-20 §2.7 的 `ApprovalPresence` 连接事实服务与「无应答方即时拒绝」，否则改判只会把安全洞换成挂到 TTL 的可用性事故。证据：`permission-effective.test.ts` Phase D 块、`permission-ask-bounds.test.ts` 「custom ceiling × responder facts (composed)」三例、`custom-child-provider-turn.test.ts` provenance 两例                             |
| canonical 工具名共享 64 字符预算，无截断/哈希策略                                              | 2026-08-25 | `mcp-remote`（短名保持原样；长名固定为 `mcp_<server-prefix>_<tool-prefix>_<hash16>`，hash 覆盖完整 server/tool 身份；最终 registry collision 仍 typed fail-closed。依据为 12 个实际 MCP 配置目录的盘点，最长 server key 为 `openai-api-key-local-confirmation`(33)，并保留 `azure-devops-work-items` + `list_work_item_comments_with_expansion`(66) 越界回归。）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Location 卸载没有可测的生产路径                                                                | 2026-08-27 | 本地 `main`（Phase G，合并 `4278b45f7`）：原判断错误，来源是没读 `LayerMap` 的接口就下结论——除了 `location-layer.ts:282` 硬编码的 `idleTimeToLive: "60 minutes"`，`LayerMap` 还有显式 `invalidate(key)`；TTL 只是自动触发器，`invalidate` 是同一套拆除的手动入口，所以**不需要为了可测性把 TTL 改成可注入**。已由 `mcp-location-isolation.test.ts` 覆盖：真 `LocationServiceMap` 起一个 Location、连上 MCP 子进程、`invalidate` 后子进程被回收、重新 `get` 同一 ref 得到干净实例。顺带测出：`LayerMap` 是引用计数的，借用未释放时 `invalidate` 只做标记、finalizer 不跑（已写进测试注释）。红证：删掉 `McpConnection.layer` 的 `addFinalizer(shutdown)` → 子进程留存                                                                                                                                                                                                                                           |
| 并发 connect 同名 server 竞态（check 与 set 之间隔着整个 spawn）                               | 2026-08-27 | 本地 `main`（合并前整改）：`mcp/connection.ts` 引入 per-server `Semaphore`，`connect` 在解码与名字校验后取锁再执行建连，`disconnect` 共用同一把锁（否则落在 `setHealth("connecting")` 与 `conns.set` 之间的 disconnect 会杀掉随后被 `establish` 重新登记的 wire，从另一侧复现同一孤儿）。红证：去掉锁后 `mcp-connection.test.ts` 的「serializes two concurrent connects for one server name and orphans no child」以 `McpRegistration.McpNameCollisionError` 失败（两个子进程都已 spawn 并各自尝试注册）                                                                                                                                                                                                                                                                                                                                                                                                       |
| MCP binding 的严格 excess-key 边界在 HTTP 层不生效                                             | 2026-08-28 | 分支 `desktop-e2e-fixes`（子代理端到端实测发现，非读 diff 发现）：`POST /custom-profile/apply` 对带 `headers:{Authorization:…}` 的 binding 返回 **200** 并静默剥掉该键 —— 正是 `McpScope` strictOptions 注释宣称要防的 silent-swallow。根因：wire JSON 不走 runtime declaration，走 `toCodecJson` link，而 link 的 **target 先解码**，target 是朴素 `McpServerBinding`（默认剥未知键）。三种修法实测：改 getter 无效（target 先剥）、删 link 则所有解码 `Expected null`（这正是 link 存在的理由）、**收紧 target 有效**。同时必须做 redaction，否则从 fail-open 换成 400 体回显调用方凭证。SDK 重跑 diff 为空。红证：还原 target → JSON 路径用例失败；去掉 redaction → 回显用例失败                                                                                                                                                                                                                            |
| `containsSecret` 漏掉带 flag 前缀与 URL 编码的凭证形态                                         | 2026-08-28 | 分支 `desktop-e2e-fixes`：实测 7 种形态逃逸（`--token=`、`--env=TOKEN=`、`?token=`、`?Authorization=Bearer%20`、`ghp_` 经 `--token=` 等），落盘后由 apply 响应与 `GET /custom-profile/content` 原样回显。根因：`ENV_LINE_RE` 是 `^` 锚定、`API_KEY_RE` 只认字面 `api_key`/`apikey`、`BEARER_TOKEN_RE` 要求 `Bearer\s+` 故对真实 URL 永不命中。补 `NAMED_CREDENTIAL_RE` + `ENCODED_BEARER_RE`。**申报的取舍**：故意不加通用高熵规则和厂商前缀大全——黑名单不可能完备，且误拒是用户直接撞墙，`credential-scan.test.ts` 钉住 5 条合法输入仍通过作为误拒预算。**更正（2026-08-29）**：此条当时只闭了 schema 一半——e4a036866 未同步 core 的 `credential-scanner.ts`，其本地 PATTERNS 数组仍是 4 项，MCP stderr 与远程响应对这四类形态不脱敏；本日已删本地数组、改遍历 schema `SECRET_PATTERN_LIST` 单一真源，`ScanResult` 的 type literals 从 4 扩到 6，`credential-scanner.test.ts` 钉住四条形态脱敏 + 同一误拒预算 |
| `consultGrant` 用 `catchCause` 吞掉中断与缺陷                                                  | 2026-08-27 | 本地 `main`（合并前整改）：`permission.ts` 删除该 `catchCause`。`ScopedGrantStore.findValid` 的错误通道是 `never`，所以这个 catch 只可能吞掉中断与 defect，违反 `core/src/tool/AGENTS.md` 明文；降级为 ask 的意图本身不需要它                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `McpConnection.disconnect` 触发 guarded lint `consistent-return`                               | 2026-08-27 | 本地 `main`（合并前整改）：`disconnect` 的生成器一条路径 `return yield* new NotConnectedError(...)`、尾部隐式 `void`，`LINT_BASE_REF=origin/main bun run script/lint-changed.ts` exit 1（CI 必红）。改为「命中 → kill + delete + `return`；未命中 → 抛 typed 错误」，两条路径都显式收束。**注意基线陷阱**：不带 `LINT_BASE_REF` 时该脚本以本地 `main` 为 diff 基线，87 个已提交改动扫不到、空绿通过，这一条正是那样被漏掉的                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 审批中心硬编码 `text-red-400`，绕过 v2 danger token                                            | 2026-08-27 | 本地 `main`（合并前整改）：`approval-center.tsx` 两处改 `text-v2-state-fg-danger`（light/dark 双绑定，`ui/src/styles/tailwind/colors.css:280`），与 `chat-import-dialog.tsx` 等既有用法一致。DESIGN.md 强制门禁：颜色一律走 Token                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 四条 MCP fail-closed 负路径零测试                                                              | 2026-08-27 | 本地 `main`（合并前整改）：`mcp_connection_unavailable`（新增 `itWithoutMcp` 无 MCP owner 的 runner 图，因 `llm.ts` 经 `Effect.serviceOption` 读它，缺失是合法宿主配置而非 Layer 错误）、`mcp_registration_mismatch`（binding 身份与 health 都匹配、fact 不含冻结的 canonical 名）、`mcp_audit_binding_missing`（catalog 与 audit 一致、tool 自身的 binding 行缺失）、`mcp_asset_not_found`（profile 仍锁 ref、资产已从磁盘删除）。前两条在 `custom-mode-drift.test.ts`，后两条在 `session-composition.test.ts` / `composition-resolver.test.ts`，均断言 turn 在 provider 派发前失败（`requests` 长度 0）                                                                                                                                                                                                                                                                                                      |
| `GrantEvent.CommitRejected` 的 defect→typed 转换无确定性故障注入                               | 2026-08-27 | 本地 `main`（合并前整改）：原判「同步 SQLite 下真 CAS miss 不可达」只对*竞态*成立，但 `publish` seam 上两条分支都可确定性驱动——`revision !== seq + 1`，以及 `commit` 回调报告未写入行。`scoped-grant-store.test.ts` 新增用例双向钉住，并用 `Cause.hasDies` 断言它到达时是 typed failure 而非 defect（这是 `grant/event.ts:49` 那层 `catchDefect` 的唯一断言）。红证：删掉该 `catchDefect` → 该用例失败                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| exerciser `v2.permission.grant.list` 场景恒绿                                                  | 2026-08-27 | 本地 `main`（合并前整改）：seed 只建 pending request、从不签发 grant，`array(body.data)` 对空数组恒真，handler 怎么坏都测不出。改用已存在的 `ctx.grantIssue` 真发一张 location grant，并断言它出现在列表里且 status/action/scope 与签发一致。coverage 硬门禁 289/289 pass 复跑确认                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| MCP remote url 未拒绝 userinfo                                                                 | 2026-08-27 | 本地 `main`（合并前整改）：`McpScope.McpServerBinding.url` 原先只校验 http(s) 前缀，`https://user:pass@host` 可通过；而该 url 被 typed connect 错误与连接投影逐字回显，这两个面按契约是无密材料的，等于一条凭证泄漏路径。新增 filter 要求 URL 可解析且 `username`/`password` 均为空（凭证只走 `credentialRef`），顺带拒掉 `https://` 这类前缀合法但不可解析的串。`mcp-scope.test.ts` 三个负例 + 一个正例                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `/mode/:mode` 无审批入口，而 presence 仍报「有人」                                             | 2026-08-27 | 本地 `main`（合并前整改）：`ApprovalCenter` 原先只挂在 session 与 draft 两条路由，`/mode/:mode` 一个都没有。**这不是「少个入口」而是接线漏了**：responder fact 由 App 那条 global SSE 流绑定（`handlers/global.ts:57`，不带 location，对任何 Location 都算有人），所以在 mode 路由上 `hasResponder()` 为真、`ask` 不 fail-closed，而是静默 park 满 TTL（默认 300s）后 `AskExpiredError`，屏幕上什么都没有——Custom 模式的主入口恰好就是 `/mode/custom`。修法：`ModeWorkspace` 把自己解析出的 directory accessor 显式传进去（该路由在 SDKProvider/DirectoryDataProvider 之外，故 `props.sdk ?? useSDK()` 短路避免无 provider 时抛错）。红证：`approval-center.spec.ts` 新增三个 e2e（custom/chat 路由各答一次 pending + 空态不渲染 trigger），去掉挂载后两个 route 用例失败                                                                                                                                      |
| 审批中心路由覆盖用源码字符串断言代替行为                                                       | 2026-08-27 | 本地 `main`（合并前整改）：上一条的首版用 `fs.readFileSync` + `toContain("<ApprovalCenter …>")` 钉挂载，直接违反 `docs/testing.md` §10 红线 3「不断言源码字符串代替行为」（违者打回）。正则也证明不了这个面在该路由上真的收到 pending。已删除这些断言，改由 e2e 在真实路由 + 真实 DOM 上驱动；组件单测只保留 `approvalScopeTransport` / `pendingForLocation` 这两个纯函数                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `permission_v2_events` 重放缓冲无界增长                                                        | 2026-08-27 | 本地 `main`（合并前整改）：该缓冲只在 `loadV2PermissionPending` 完成时清空，若某 Location 的 bootstrap 不再完成（离线、或 App 不再重新 bootstrap 的目录），两处 append 会把它涨到标签页生命周期结束。加 500 条上限、淘汰最旧（load 只读取比它启动时 revision 更新的事件，需要的永远在尾部）。**申报的 tradeoff**：跨越 500 条事件仍未完成的 load 可能丢一条、少报一个 pending，未做 per-load 水位线以免新增 store 状态。`event-reducer.test.ts` 钉住上限、尾部保留与 pending 投影不受影响                                                                                                                                                                                                                                                                                                                                                                                                                      |
| HttpApi exerciser 的 auth 硬门禁无有效信号（0 pass / 289 fail）                                | 2026-08-27 | 本地 `main`（合并前整改，**既有失败、非本分支引入**，但它是 `test.yml:81` 无 `continue-on-error` 的硬门禁，不修则 PR 必红）。根因：`withContext` 会 `Layer.buildWithMemoMap(AppLayer, memoMap, scope)`，TaskDriver runtime state 就在 `AppLayer` 里；而 `runAuth` 整个跳过 `withContext`，于是 auth 模式下**没有任何 scenario 往共享 memoMap 建过这一层**，每个探针（连公开的 `/global/health`）都死在 `TaskDriver runtime state is not provided`。修法：让 `runAuth` 做与正常路径同一次 build（`buildWithMemoMap` 会 memo，只有第一个 scenario 付代价）。现 **289/289 pass**。红证：把 `basic("aigcfroge", "secret")` 改成错密码 → 289 全红，证明 401 / 非 401 断言是活的而非恒真                                                                                                                                                                                                                             |
| 生成的 V2 SDK 与生成器输出一致性只有人工结论                                                   | 2026-08-27 | 本地 `main`（合并前整改）：原判「重跑生成器需起 server + 联网」是错的——`bun ./packages/sdk/js/script/build.ts` 离线即可跑（内部 `bun dev generate` 直接产 1.6MB spec，exit 0，约 40s）。已重跑，`git diff -- packages/sdk/js/src/v2/gen` **为空**，提交的生成物与生成器输出逐字一致，grant 端点一致性不再是人工比对结论                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

| 全仓 Prettier 基线漂移无门禁（§3.2 与 §4 重复登记，合并为一条） | 2026-08-30 | 分支 `ci-gates-s5`（S5 B8，同批两独立提交，缺一即再漂）：提交 A 全仓 `prettier --write`（format-only 零语义，Phase A 实测 436 个未过文件一次归位）；提交 B 把 `script/format.ts --check` 接入 `bun run lint` 链路（oxlint 之后、lint-changed 之前）。此后本条按「有无门禁」登记、不按计数：再漂会以 lint 红显形 |
| `bun turbo test` 只跑 5 个包，6 个包共 775 个用例在 CI 从不执行 | 2026-08-30 | 分支 `ci-gates-s5`（S5 B1）：`packages/desktop/package.json` 补 `"test": "bun test --timeout 30000 --only-failures"`（照 tui/llm 既有形式），`turbo.json` 补 6 个 `@aigcfroge/<name>#test`、`dependsOn: ["^build"]` 与既有 5 条对齐；`bunx turbo test --dry=json` 实测 test 任务 5→11。红证对照：Phase A 向 6 个包各塞一条必失败断言时 `bun turbo test` 仍全绿，补齐后同样种红变红 |
| `test/server/` 硬门禁里有一条依赖 github.com 的用例，网络不通即红 | 2026-08-30 | 分支 `ci-gates-s5`（S5 B6）：`httpapi-reference.test.ts` 改用本地 fixture（临时目录 `git init` 裸仓库，reference 路径指向本地而非 `Global.Path.repos/github.com/...`），选择依据写进用例注释——本地 fixture 保留「真 clone + 真 poll 就绪」的行为断言，强于改 advisory。实测正常网络与 `HTTPS_PROXY=http://127.0.0.1:1` 断网两种环境均绿 |
