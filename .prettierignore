sst-env.d.ts
packages/desktop/src/bindings.ts

# Nested git worktrees (subagent checkouts). They are hidden from `git status`
# by .git/info/exclude, but Prettier does not read that file — so
# `script/format.ts` (`prettier --write .`, which script/generate.ts runs at the
# end) reformats every file inside them. That rewrite lands in the OTHER
# worktree, so the main tree still looks clean while SDK generation appears to
# have reformatted the whole repo.
.claude/worktrees/

# Local build/test artifacts. Prettier does not read .gitignore (same situation
# as .claude/worktrees/ above), so any local desktop/storybook/e2e run turns
# `bun run lint` red on files git check-ignore confirms are untracked.
packages/app/e2e/test-results/
packages/app/e2e/playwright-report/
packages/app/e2e/real/test-results/
packages/app/e2e/real/playwright-report/
packages/desktop/out/
packages/storybook/storybook-static/

packages/app/e2e/real/run-evidence/
# Generated contract snapshot; drift is gated by openapi-drift.test.ts.
packages/aigcfroge/test/server/openapi.snapshot.json
