name: aigcfroge

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  aigcfroge:
    if: |
      contains(github.event.comment.body, ' /oc') ||
      startsWith(github.event.comment.body, '/oc') ||
      contains(github.event.comment.body, ' /aigcfroge') ||
      startsWith(github.event.comment.body, '/aigcfroge')
    runs-on: blacksmith-4vcpu-ubuntu-2404
    permissions:
      id-token: write
      contents: read
      pull-requests: read
      issues: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: ./.github/actions/setup-bun

      - name: Run aigcfroge
        uses: ./github
        env:
          AIGCFROGE_API_KEY: ${{ secrets.AIGCFROGE_API_KEY }}
          AIGCFROGE_PERMISSION: '{"bash": "deny"}'
        with:
          model: aigcfroge/claude-opus-4-5
